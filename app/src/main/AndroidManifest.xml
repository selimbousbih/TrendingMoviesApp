<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.selim.trends">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.MovieTrendsApplication"
        android:allowBackup="true"
        android:icon="@drawable/mt_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mt_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieTrends">
        <activity
            android:name=".features.details.DetailsActivity"
            android:exported="false"
            android:theme="@style/Theme.MovieTrends.NoActionBar" />
        <activity
            android:name=".features.home.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>